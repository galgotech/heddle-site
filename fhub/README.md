# Fhub

Fhub is a code repository and collaboration platform that manages atomic **Functions** as the primary unit of value. It is a "Function Hub."

This repository contains the source code for the Fhub platform, which is being built with a Go backend and a React frontend.

## Documentation

- **[Architecture Overview](docs/ARCHITECTURE.md):** A high-level overview of the Fhub platform's architecture.
- **[Database Schema](docs/SCHEMA.md):** The database schema for the Fhub platform.

## Getting Started

> **Note:** This project is currently in the design and initial setup phase. The instructions below are placeholders for when the project is further along.

### Prerequisites

- Go (latest version)
- Node.js (LTS version)
- Docker (for running the database and other services)

### Backend

To run the backend services:

```bash
# To-do: Add instructions for running the backend
```

### Frontend

To run the frontend development server:

```bash
cd frontend
npm install
npm run dev
```
